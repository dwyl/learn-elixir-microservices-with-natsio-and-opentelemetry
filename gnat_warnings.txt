Compiling 24 files (.ex)
     warning: a struct for Gnat.Jetstream.API.Consumer is expected on struct update:

         %Gnat.Jetstream.API.Consumer{consumer | deliver_policy: :by_start_sequence, opt_start_seq: seq}

     but got type:

         dynamic()

     where "consumer" was given the type:

         # type: dynamic()
         # from: lib/gnat/jetstream/pager.ex:110:31
         consumer

     when defining the variable "consumer", you must also pattern match on "%Gnat.Jetstream.API.Consumer{}".

     hint: given pattern matching is enough to catch typing errors, you may optionally convert the struct update into a map update. For example, instead of:

         user = some_function()
         %User{user | name: "John Doe"}

     it is enough to write:

         %User{} = user = some_function()
         %{user | name: "John Doe"}

     typing violation found at:
     │
 116 │         %Consumer{consumer | deliver_policy: :by_start_sequence, opt_start_seq: seq}
     │         ~
     │
     └─ lib/gnat/jetstream/pager.ex:116:9: Gnat.Jetstream.Pager.apply_opts_to_consumer/2

    warning: a struct for Gnat.Jetstream.API.Object.Meta is expected on struct update:

        %Gnat.Jetstream.API.Object.Meta{meta | deleted: true}

    but got type:

        dynamic()

    where "meta" was given the type:

        # type: dynamic()
        # from: lib/gnat/jetstream/api/object.ex:51:22
        {:ok, meta} <- info(conn, bucket_name, object_name)

    when defining the variable "meta", you must also pattern match on "%Gnat.Jetstream.API.Object.Meta{}".

    hint: given pattern matching is enough to catch typing errors, you may optionally convert the struct update into a map update. For example, instead of:

        user = some_function()
        %User{user | name: "John Doe"}

    it is enough to write:

        %User{} = user = some_function()
        %{user | name: "John Doe"}

    typing violation found at:
    │
 52 │          meta <- %Meta{meta | deleted: true},
    │                  ~
    │
    └─ lib/gnat/jetstream/api/object.ex:52:18: Gnat.Jetstream.API.Object.delete/3